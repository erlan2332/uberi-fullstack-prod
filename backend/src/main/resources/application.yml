spring:
  application:
    name: uberi-spring-backend

server:
  port: 8082
  compression:
    enabled: true
    min-response-size: 1024
    mime-types: application/json,text/plain,text/css,text/html,application/javascript

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,https://uberi-frontend-erlan2332.fly.dev}

telegram:
  bot-token: ${TELEGRAM_BOT_TOKEN:8721133514:AAFx8v7wUq4-QM-iewjWWPpQn1xX3yiGbmY}
  # Для канала нужен публичный username (например @uberi) или chat id вида -100...
  # Инвайт-ссылка формата https://t.me/+... не является chat_id.
  # Этот chat-id используется как резервный получатель.
  # Дополнительно заявки отправляются всем личным чатам, где пользователи нажали /start у бота.
  chat-id: ${TELEGRAM_CHAT_ID:1664044922}
